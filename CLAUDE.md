# SnowGauge — project context for Claude

Low-cost, low-power snow depth logger (NIR laser ToF + XIAO nRF52840 Sense). Field trial starts 2026-12-31; student assembly session on 2026-09-17.

## Single sources of truth

- Requirements/architecture: `SnowGauge_設計仕様書_v0.11.md` (Japanese; full-revision policy — bump the version on every change)
- **Pin map & electrical topology: `pcb/README.md`** (PCB v1.2). Anything else (older docs, §4 of the spec) is superseded where they disagree.
- PCB is generated by `pcb/generate_board.py` (KiCad 10 pcbnew script). Never hand-edit `SnowGauge.kicad_pcb`; edit the script, regenerate, run DRC via kicad-cli, re-export gerbers.

## Current state (2026-09-03, session 2)

**Hardware**
- PCB v1.2 ordered at JLCPCB (64×50mm, gerbers = `pcb/SnowGauge_v1.2_gerbers.zip`), ETA ~2026-09-10. Two independent design reviews passed. Known accepted margins: RX/Q2-pad clearances ~0.37mm; ADC reads Vbat/2 ≈3.5V max on fresh cells.
- **Breadboard is fully built and verified (STEP 1–5)**. Guide: `docs/breadboard_guide.html`, generated by `docs/gen_breadboard.py` (never hand-edit the HTML; edit `PARTS` and rerun). XIAO sits USB-left, which puts **top row (e20–e26) = 5V,GND,3V3,D10,D9,D8,D7 and bottom row (f20–f26) = D0/A0…D6** — the old guide had this swapped. Resistor substitutes on the board: R3=150Ω, R4=R5=10k, R1=R2=5k, R7=2.2k (note in guide §4). Bench PSU 6.0 V replaces the L91 pack; **picowatt** (user's INA228 tool, `~/NIES/picowatt`, port `/dev/cu.usbmodem214xx`) sits in the supply line.
- Bench rule: USB and battery/PSU may coexist only with jumper ⑩ (a10→a22, U1 3.3 V → XIAO 3V3) pulled. Flashing needs USB (⑩ out); sleep tests need USB out and ⑩ in. At session end the board was on PSU only with ⑩ in.

**Firmware (`firmware/`, NCS v3.4.0)** — steps 1–3 done and measured; **step 4a (record storage) done and verified on the breadboard** (records persist across reflash/reset, clock restored as ESTIMATED, erase, decoder validated against device bytes). Sleep current with 4a: **18 µA ±10** (unchanged). **Step 4b (BLE status advertising) done and measured: +8 µA → 26 µA ±10 with adv 1–2 s. Step 4c (SMP/mcumgr) done and verified**: Echo + file download from the Android nRF Connect Device Manager (its Files tab hard-codes `/lfs1/`, so LittleFS is mounted there), datetime get/set verified over the USB shell transport with `tools/smp_datetime.py` (clock → SYNCED). Device Manager has no datetime UI; the field procedure for time sync is still open (options: a tiny custom GATT characteristic in 4d, or a phone app that supports os datetime). **4d firmware flashed 2026-09-04 and shell-verified** (schedule set/persist, `cal zero 30` → d0 persists after reboot; fix: `settings_save_subtree("sg/cal")` matches only the handler name "sg" → save the cal keys with `settings_save_one`). Page fully verified from the Android phone 2026-09-04 (sync, settings, live, ZERO/depth reference, download/CSV, erase). Modules: `config.c` (settings `sg/sched/*`, `sg/cal/*`, scheduler math), `cal_gatt.c` (custom service), scheduler loop in `main.c`, shell `cfg show|sched`, `cal zero|clear|live`. **Web Bluetooth page live on GitHub Pages: https://0kam.github.io/SnowGauge/app/** (Pages enabled 2026-09-04; pushes to main redeploy in ~1 min). First phone test against the 4c build: connect/echo/download OK, everything else needed 4d. Remaining test list: `cfg sched 17:00 05:00 90` → `cfg show` shows the next slot; `cal zero` → d0/theta0; `reboot` → settings persist; SMP settings read of `sg/sched/interval_min` from the page; live/ZERO/ERASE from the page's calibration panel (open the page in Chrome on the Mac: `python3 -m http.server 8787 --directory docs/app`, or on the Android phone once it is on GitHub Pages); then PSU-only sleep current (settings/NVS and the GATT service should add nothing). Next after that: 5 (TSD20).
- Step 4a = `record.c` (40-B record v1 + CRC-16, `docs/record_format.md`), `storage.c` (LittleFS `/lfs1/rec_YYYYMM.bin` on the QSPI + raw append-only mirror in a new internal-flash partition 0x9F000–0xEC000; app partition shrunk to 480 kB), `timekeeping.c` (rtc-emul behind the `rtc` alias, UNSET/ESTIMATED/SYNCED; re-seeded from the newest record at boot). QSPI deep power-down is now done by the driver's runtime PM (`zephyr,pm-device-runtime-auto`, dwell 50 ms) instead of the manual suspend in `power.c`. Shell: `rec count|ls|dump|hex|erase ERASE`, `time [set <epoch>]`, `reboot`, `ble [adv <min> <max>]`; SMP also on the shell port (`tools/smp_datetime.py`, smpclient); `measure` now stores a record. Host tools: `tools/decode_records.py` (bin → CSV, self-tested), `tools/sgshell.py` (pyserial shell driver).
- 4a verified 2026-09-03 with `tools/sgshell.py`: `time set`, `measure` ×3 → `/lfs1/rec_202609.bin` 120 B, mirror 3 slots; DFU reflash + `reboot` keep the records, seq continues, clock comes back ESTIMATED; `rec erase ERASE` clears both; `rec hex` → `tools/decode_records.py --hex` decodes (Zephyr `crc16_ccitt` is the *reflected* CCITT, poly 0x8408). Review fix worth remembering: `CONFIG_PM_DEVICE_RUNTIME_DEFAULT_ENABLE=n` — otherwise the UARTE driver re-activates the TFmini UART (TX high) with the rail off. The clock is uptime-based (rtc-emul drifts ~2.6 s/day; it is only mirrored for mcumgr).
- Verified (steps 1–3): rail switch, TFmini 100 Hz burst (median/variance/strength/temp, 0 checksum errors), battery ADC, IMU tilt (LSM6DS3TR-C, supply-gated), QSPI deep power-down, VBUS-gated USB, shell over USB CDC (`rail`, `tfmini`, `batt`, `tilt`, `measure`, `auto`, `dfu`, `reboot`, `rec`, `time`, `device list`, `sensor`).
- **Measured on the breadboard** (`docs/measurements/2026-09-03_breadboard_current.md`): sleep **17 µA ±10** (whole system), measurement burst 1.58 s / 0.037 mAh, i.e. ≈21 µA average before BLE. The 1.89 mA trap: Zephyr's udc_nrf keeps HFXO+USBD on whenever the USB device is enabled, regardless of VBUS → `usb_pm.c` enables it only while VBUS is present (`CONFIG_CDC_ACM_SERIAL_ENABLE_AT_BOOT=n`).
- IMU gotcha: board regulator delay (3 ms) is too short → `zephyr,deferred-init` on the sensor node and `device_init()` after 30 ms in `tilt_init()`.
- The board currently runs the **step 4c build (shell only, BLE advertising + SMP on)**; the old note: the **auto-60 s test build** (`-DEXTRA_CONF_FILE=overlay-auto60.conf`, measures every 60 s, green LED pulse). Default build has auto period 0 (shell only). `CONFIG_SENSOR_SHELL`/`CONFIG_DEVICE_SHELL` are left on for debugging (~13 KB flash).
- TFmini "chip temp" (~50–75 °C) is the sensor's internal temperature; use the IMU temperature as the environment proxy.

**Step 4 plan (agreed)**: (a) record format + LittleFS + mirror + clock — **done**; (b) BLE advertising with Manufacturer Data — **done** (`ble_adv.c`: name `SG-XXXX`, company 0xFFFF, 16-byte payload documented in `ble_adv.h`; shell `ble [adv <min> <max>]`; +8 µA measured); (c) SMP/mcumgr fs + os (datetime) — **done** (`smp_mgmt.c` datetime hooks; SMP over BLE and over the USB shell); (d) custom calibration GATT (live stream, ZERO, ERASE). Then step 5 (TSD20 driver variant).

**Robustness pass 2026-09-04** (independent review, all High + key Medium fixed, commit a533f1a): sensor mutex (`measure.h sensor_lock`), Vbat < 4600 mV skips the TFmini burst, boot hold-off 10 min × consecutive-reset count (GPREGRET2), calibration commands on their own work queue with busy/seq in the status characteristic (page polls), live resumes after ZERO only while connected, BLE ERASE refused until a record file was read over SMP since boot, clock set rejected outside 2026..2100, settings validated before commit; page serializes SMP requests, drops the RX buffer on timeout, locks all buttons during an action, two-tap needs ≥1.5 s between taps, service worker is offline-first. **Deferred**: site.json torn upload (store lat/lon in settings too), mirror erase failure handling, quick-reconnect retry, missed slot during live mode, tz change confirmation.

**Open items**
- Publish `docs/app/` on GitHub Pages (user action: repo Settings → Pages → branch main, folder /docs; URL becomes https://0kam.github.io/SnowGauge/app/). Until then test from localhost on the Mac.
- BLE gotcha fixed 2026-09-03: legacy advertising did *not* resume after a disconnect (device said adv=on, phone saw nothing) → `ble_adv.c` stops+restarts from a work item on disconnect.
- WDT (spec §12.3) still off: the nRF52 WDT keeps running across a soft reset into the Adafruit bootloader, so enable it only with a long timeout and verify the `dfu` path still works.
- Time after a reset is only an estimate (restored from the last record); the mcumgr datetime sync at each visit (4c) fixes it going forward. Consider persisting time periodically in `storage_partition` if the drift after WDT resets matters.
- Spec bump to v0.12 pending: §7.1 with measured numbers (after PCB re-measurement), breadboard notes. Spec v0.11 already records the UF2-only / no-MCUboot decision.
- `~/NIES/picowatt` has an **uncommitted** agent-made change (`app/src/picowatt/cli.py`: per-channel vbus/current summary + "vbus unstable → check GND link" warning; pytest passes). User to decide whether to commit. Root cause of the 18 V VBUS reading was a missing PSU(−)–Pico GND link, not software.
- Student assembly session 2026-09-17: the breadboard guide + firmware README are the hand-outs so far.

**Tooling on this Mac**
- NCS v3.4.0 at `/opt/nordic/ncs/v3.4.0` (`~/.local/bin/nrfutil sdk-manager`). Build: `cd /opt/nordic/ncs/v3.4.0 && nrfutil sdk-manager toolchain launch --ncs-version v3.4.0 -- west build -d <repo>/firmware/build -b xiao_ble/nrf52840/sense <repo>/firmware [-- -DEXTRA_CONF_FILE=overlay-auto60.conf]`. Output `firmware/build/firmware/zephyr/zephyr.{hex,uf2}`.
- **Flashing**: macOS 26 cannot mount the XIAO's Adafruit-bootloader UF2 drive. Use serial DFU: `adafruit-nrfutil dfu genpkg --dev-type 0x0052 --sd-req 0xFFFE --application zephyr.hex pkg.zip`; put the board in the bootloader (shell `dfu`, or double-tap reset); `adafruit-nrfutil dfu serial --package pkg.zip -p /dev/cu.usbmodem21101 -b 115200 --singlebank`. `--sd-req 0x00` makes the bootloader reject and reset mid-transfer. adafruit-nrfutil + pyserial live in a scratch venv (`pip install adafruit-nrfutil pyserial`).
- Shell driving: a tiny pyserial script sending commands to `/dev/cu.usbmodem21101` at 115200 works; strip ANSI codes.
- picowatt capture: `cd ~/NIES/picowatt/app && uv run picowatt-cli --port /dev/cu.usbmodem214xx --mode single --adcrange 1 --preset very-quiet --seconds N --csv out.csv` (7.6 Sa/s, 2 µA LSB). µA-level absolute values need the difference method described in the measurements doc.
- MCUboot would overwrite the Adafruit bootloader → needs SWD; decided against for v1 (spec v0.11).

## Step 4d design (agreed 2026-09-03; supersedes the "custom GATT only" idea for settings)

- **Phone/PC UI = one Web Bluetooth page** (`docs/app/` → GitHub Pages, PWA-cached; Android Chrome / desktop Chrome; iPhone needs Bluefy). It speaks **SMP in JS** (os datetime/echo/reset, fs upload/download, settings mgmt). Generic core reusable as the base for other loggers; project-specific parts are the record schema, the settings schema and the calibration panel.
- **Schedule** (Zephyr settings, SMP settings group; if the clock was never set the device still measures every `interval_min` from boot into `rec_notime.bin` — verified 2026-09-04): `sg/sched/start_min`, `end_min` (local minutes-of-day, start > end = spans midnight, start == end = all day), `interval_min`, `tz_min` (UTC offset; the device clock stays UTC). Bench `auto <s>` overrides.
- **Site info** = `/lfs1/site.json` written by the page via fs upload (lat/lon/alt/accuracy/source/set_at/tz/name + history array). Not per record.
- **Sync button** sends time + tz + location together (individual actions also available).
- **Calibration GATT** (custom service): live notify (dist/strength/tilt/vbat), control write (live on/off, ZERO 0x10, reference-from-probed-depth 0x11+u16 cm, ERASE+token), status read (d0, theta0, set epoch). Reference: `d0 = d + depth/cos(tilt)` stored in `sg/cal/*`.
- **UI rules (user, 2026-09-03)**: gloves + small screen → big buttons, pickers only (no typed numbers except the optional manual lat/lon), no UI for values the page/device decides (tz auto from phone, d0/θ0 display only), destructive actions = two taps.
- **CSV export** done in the page: one row per record, header row, site columns repeated per row, `time_utc` + `time_local` ISO 8601, derived `d0_cm, theta0_deg, snow_depth_cm` at the end; raw .bin also saveable. Column list in `docs/record_format.md`.
- Open: no pairing → anyone nearby can erase/change settings (ERASE has a token; PIN later if needed).

## Firmware decisions (spec §12, closed)

- **nRF Connect SDK (Zephyr)**, board `xiao_ble/nrf52840/sense`. **Flashing is USB/UF2 only (decided 2026-09-02, no SWD probe): keep the Adafruit bootloader, no MCUboot, BLE DFU deferred to a future revision.**
- BLE: SMP/mcumgr (LittleFS file download + datetime; img mgmt/DFU off) → works with nRF Connect Device Manager app (iOS/Android). Custom GATT only for calibration live-stream/ZERO/ERASE. Advertising carries battery/record-count/last-distance in Manufacturer Data.
- Records: LittleFS on 2MB QSPI (deep power-down when idle!), mirror to internal flash. IMU (LSM6DS3TR-C) behind a `TiltSensor` abstraction.
- Key GPIO (v1.2): **D10=SENSOR_EN** (high = sensor rail on), **A0=battery divider** (enable SENSOR_EN first, read ×2; node is 0V in sleep — D3/Q3 no longer exist), D6/D7=UART to TFmini (**set TX Hi-Z/low before cutting the rail** — ghost-powering), D4/D5 spare I2C.
- Power budget: sleep ≤90µA worst (target ~50µA); measure 8×/day, N samples → median+variance+quality flags (TFmini sentinel vs TSD20 sentinel 50000).

## FW development order (agreed with user)

1. Zephyr scaffold + TFmini UART test FW (breadboard STEP 5 check uses it)
2. Measurement sequence (rail switch, N-sample median/variance, IMU tilt, battery ADC)
3. Sleep-current tuning (QSPI power-down, IMU off, BLE adv 1–2s)
4. BLE (SMP + custom calibration service), record storage
5. TSD20 driver variant (frame format + sentinel differ; same board with U2=F33)

## Conventions

- Chat in Japanese; code/comments in English. Commit messages in English.
- User runs purchases/orders themselves; prepare artifacts (gerber zips, BOMs) for them.
